import CreateUser from "@/components/company/users/CreateUser";

export const metadata = {
  title: "User Create",
};

const CreateUserPage = () => {
  return (
    <main>
      <CreateUser />
    </main>
  )
}

export default CreateUserPage;